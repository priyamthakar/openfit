# cli

The `openfit.cli` module implements the `openfit` command-line interface. It exposes sub-commands for fitting CSV files, re-running fits from a FitSpec, listing available models, and generating reports. Requires the `[cli]` install extra.

::: openfit.cli
