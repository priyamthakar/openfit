# models

The `openfit.models` module contains the model registry and all 29 built-in model definitions. Use `list_models()` to enumerate available models, or register a custom callable with `register_model()`.

::: openfit.models
