# fit

The `openfit.fit` module provides the central `Fit` class, which is the primary entry point for all curve fitting in openfit. Instantiate it with a model name, x/y data, and a weights expression to obtain a `FitResult`.

::: openfit.fit
