# global_fit

The `openfit.global_fit` module enables global (simultaneous) fitting of a model to multiple datasets with one or more parameters shared across all datasets. This is useful for fitting binding curves, dose-response series, or kinetic traces where a physical parameter is expected to be constant across conditions.

::: openfit.global_fit
