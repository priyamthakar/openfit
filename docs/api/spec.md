# spec

The `openfit.spec` module defines `FitSpec`, the reproducibility manifest that records the data hash, model, weights, solver settings, and software versions for a fit. Specs can be saved to JSON and used to re-run a fit exactly.

::: openfit.spec
