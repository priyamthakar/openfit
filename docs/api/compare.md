# compare

The `openfit.compare` module provides tools for comparing two or more fitted models using information criteria (AIC, BIC) and, for nested models, the F-test. Pass a list of `FitResult` objects to `compare()` to receive a ranked comparison table.

::: openfit.compare
