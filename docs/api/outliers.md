# outliers

The `openfit.outliers` module implements ROUT (Robust Regression and Outlier Removal), the method described by Motulsky & Brown (2006). This is the first Python implementation of ROUT. It identifies outliers relative to a robust fit before running the final weighted least-squares fit.

::: openfit.outliers
