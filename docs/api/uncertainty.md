# uncertainty

The `openfit.uncertainty` module implements profile-likelihood confidence interval computation. Profile-likelihood CIs are asymmetric and statistically rigorous, making them preferable to symmetric asymptotic (SE-based) intervals for nonlinear models.

::: openfit.uncertainty
