# io

The `openfit.io` module handles reading data from CSV and other tabular formats, and writing HTML, PDF, and DOCX reports. It also provides helpers for loading and saving `FitSpec` JSON files.

::: openfit.io
