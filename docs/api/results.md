# results

The `openfit.results` module defines `FitResult`, the object returned by every successful fit. It exposes fitted parameters, standard errors, confidence intervals, goodness-of-fit statistics, and methods for reporting and spec export.

::: openfit.results
